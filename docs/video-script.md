# Demo video script — working outline

Target length: 90–120 seconds. Record only verified behavior.

## 0:00–0:15 — Problem

“A single project update can invalidate tasks, milestones, risks, and artifacts. Teams usually discover the chain reaction manually.”

Show the synthetic project baseline and clearly identify it as fixture data.

## 0:15–0:35 — Evidence

Paste the exact venue update. Show the raw source beside the structured candidate change. Explain that GPT-5.6 interprets the update but does not change project records.

## 0:35–0:55 — Impact

Show direct impacts, then open one multi-hop dependency path. Explain that application code traverses explicit edges and the model does not decide reach.

## 0:55–1:20 — Proposal and approval

Review the recovery draft. Approve low-risk actions individually and leave travel rebooking unapproved. State that a proposal is not permission.

## 1:20–1:40 — History, undo, reset

Show operation history, undo, and reset only if each works in the recorded build. Otherwise omit this segment and describe it as the next P0 milestone.

Before recording, complete the live production smoke path in `docs/qa-checklist.md`. Do not record the guarded CI-only fixture as if it were a live OpenAI/Supabase result, and keep credentials, cookies, headers, environment values, provider metadata, and private payloads out of the capture.

## Close

“InOrdo keeps the chain visible: evidence, impact, approval, and a way back.”
